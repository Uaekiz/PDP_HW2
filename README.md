for cpp change the path below the code then 
to compile: "g++ matrix_benchmark.cpp -o matrix_benchmark.exe"
to run ".\matrix_benchmark.exe" OR "./matrix_benchmark.exe" it depends to your terminal

for java change the path below the code then 
to compile: "javac MatrixBenchmark.java"
to run "java MatrixBenchmark"

for py change the path below the code then 
to run "python matrix_benchmark.py"